# Backend itself also runs inside docker-compose, talks to MySQL over the compose network
spring.datasource.url=jdbc:mysql://mysql:3306/ironforge?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:ironforge}
spring.datasource.password=${DB_PASSWORD:ironforge123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
